import config from "../compoment/utls/config";
import ajax from "ajax";

//url前缀
const urlPre = config.host + "/categoriesApi";

/**
 * 根据cid获取板块名称
 * @param cid
 */
export function getNameById(cid) {

}