export const title = ({index_header}) => index_header.title;

export const totalMembers = ({index_header}) => index_header.totalMembers;

export const totalArticles = ({index_header}) => index_header.totalArticles;

export const isShowHeaderDetail = ({index_header}) => index_header.isShowHeaderDetail;
